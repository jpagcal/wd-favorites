import {Box} from 'ink';

const StyledBox = () => {
	return (
		<Box
			flexDirection="column"
			padding={2}
			borderStyle="bold"
			borderDimColor
			gap={2}
		></Box>
	);
};

export default StyledBox;
